name: tomy_app
description: تطبيق تومي لتتبع الجروب بالخريطة الحية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0
  battery_plus: ^6.0.0
  url_launcher: ^6.2.0
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
